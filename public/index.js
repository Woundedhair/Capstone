function handleSubmit(evt) {
	alert('Form submitted successfully');
};

const button = document.querySelector('#add');
function button() {
    prompt('Add a new RRR')
};

let form = document.querySelector('#pReview');
let forms = document.querySelector('#sReview');
let formed = document.querySelector('#bReview');
let mform = document.querySelector('#mReview');
let kform = document.querySelector('#kReview');
let sform = document.querySelector('#sReview');

form.addEventListener('submit', handleSubmit);
forms.addEventListener('submit', handleSubmit);
formed.addEventListener('submit', handleSubmit);
mform.addEventListener('submit', handleSubmit);
kform.addEventListener('submit', handleSubmit);
sform.addEventListener('submit', handleSubmit);
button.addEventListener('click', button);